#include<stdio.h>
#include<string.h>
int main()
{
    char str[50]="rashii";
    printf("the given string is=%s\n",str);
    printf("after reversing string is=%s",strrev(str));
    return 0;
}
